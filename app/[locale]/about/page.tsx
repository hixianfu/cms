import type { Metadata } from "next";
import Image from "next/image";
import Link from "next/link";
import { ArrowRight, Sparkles } from "lucide-react";
import { notFound } from "next/navigation";
import { RichTextRenderer } from "@/components/content/RichTextRenderer";
import { getAbout, getGlobal } from "@/lib/strapi/queries";
import { isLocale } from "@/lib/i18n/config";
import { createMetadata } from "@/lib/seo/metadata";
import { resolveMediaUrl } from "@/lib/strapi/image";
import { localizedHref } from "@/lib/i18n/routing";

export async function generateMetadata({ params }: { params: Promise<{ locale: string }> }): Promise<Metadata> { const { locale } = await params; if (!isLocale(locale)) return {}; const [about, global] = await Promise.all([getAbout(locale), getGlobal(locale)]); return createMetadata(about?.seo, { title: about?.title ?? (locale === "zh" ? "关于我们" : "About us"), description: global?.siteDescription ?? undefined }); }
export default async function AboutPage({ params }: { params: Promise<{ locale: string }> }) { const { locale } = await params; if (!isLocale(locale)) notFound(); const about = await getAbout(locale); if (!about) return <section className="mx-auto max-w-3xl px-4 py-24 text-center"><h1 className="text-3xl font-semibold">{locale === "zh" ? "关于我们" : "About us"}</h1><p className="mt-4 text-slate-600">{locale === "zh" ? "内容暂不可用" : "Content unavailable"}</p></section>; const cover = resolveMediaUrl(about.cover); const zh = locale === "zh"; return <article className="relative overflow-hidden bg-[linear-gradient(180deg,#eef5f7_0%,#f7fafb_32rem,#f7fafb_100%)]"><div aria-hidden="true" className="pointer-events-none absolute -right-32 top-[-8rem] h-96 w-96 rounded-full bg-brand-teal/10 blur-3xl" /><div className="relative mx-auto max-w-[1720px] px-4 pb-20 pt-8 sm:px-6 sm:pt-12 lg:px-8 lg:pb-28"><nav aria-label={zh ? "面包屑导航" : "Breadcrumb navigation"} className="flex items-center gap-2 text-sm text-brand-muted"><Link href={localizedHref(locale, "/")} className="transition hover:text-brand-blue">{zh ? "首页" : "Home"}</Link><ArrowRight size={14} className="text-brand-teal" aria-hidden="true" /><span aria-current="page" className="font-semibold text-brand-ink">{zh ? "关于我们" : "About us"}</span></nav><header className="mt-10 grid overflow-hidden rounded-[2rem] bg-brand-ink text-white shadow-[0_24px_70px_rgba(18,50,74,0.16)] lg:grid-cols-[minmax(0,0.88fr)_minmax(0,1.12fr)]"><div className="relative flex flex-col justify-center px-6 py-12 sm:px-10 sm:py-16 lg:px-16 lg:py-20"><div aria-hidden="true" className="absolute -left-24 bottom-[-8rem] h-72 w-72 rounded-full bg-brand-blue/35 blur-3xl" /><p className="relative flex items-center gap-3 text-xs font-semibold uppercase tracking-[0.24em] text-brand-teal sm:text-sm"><Sparkles size={15} />{zh ? "关于我们" : "About us"}</p><h1 className="relative mt-5 max-w-xl text-4xl font-semibold leading-[1.08] tracking-[-0.03em] sm:text-5xl lg:text-6xl">{about.title}</h1><p className="relative mt-6 max-w-lg text-base leading-8 text-slate-300 sm:text-lg">{zh ? "了解我们的理念、能力，以及持续为客户创造价值的方式。" : "Discover the people, capabilities, and principles behind the value we create for customers."}</p><Link href={localizedHref(locale, "/contact")} className="group relative mt-8 inline-flex w-fit items-center gap-3 rounded-full bg-brand-lime px-5 py-3 text-sm font-bold text-brand-ink transition hover:-translate-y-0.5 hover:bg-white">{zh ? "联系我们" : "Talk to our team"}<ArrowRight size={16} className="transition group-hover:translate-x-1" /></Link></div>{cover ? <div className="relative min-h-[20rem] overflow-hidden bg-slate-800 sm:min-h-[26rem] lg:min-h-full"><Image src={cover} alt={about.coverAlt ?? about.cover?.alternativeText ?? about.title} fill priority sizes="(min-width: 1024px) 60vw, 100vw" className="object-cover transition duration-700 hover:scale-105" /><div className="absolute inset-0 bg-gradient-to-r from-brand-ink/45 via-transparent to-transparent lg:from-brand-ink/70" /></div> : <div className="min-h-64 bg-[linear-gradient(135deg,#164776,#009ea2)]" />}</header><div className="mt-10 grid gap-8 lg:grid-cols-[minmax(0,1fr)_18rem] lg:items-start"><section className="min-w-0 rounded-[1.75rem] border border-brand-border/80 bg-white/85 p-6 shadow-[0_18px_50px_rgba(18,50,74,0.06)] sm:p-10 lg:p-14"><div className="mb-8 border-b border-brand-border/80 pb-6"><p className="text-xs font-semibold uppercase tracking-[0.22em] text-brand-teal">{zh ? "我们的故事" : "Our story"}</p><h2 className="mt-2 text-2xl font-semibold text-brand-ink sm:text-3xl">{zh ? "以专业能力，持续创造长期价值" : "Building lasting value through expertise"}</h2></div><div className="prose prose-slate max-w-none leading-8"><RichTextRenderer blocks={about.blocks} /></div></section><aside className="space-y-5 lg:sticky lg:top-24"><div className="rounded-[1.5rem] bg-white/85 p-6 shadow-[0_18px_45px_rgba(18,50,74,0.08)]"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-brand-teal">{zh ? "探索更多" : "Explore more"}</p><div className="mt-4 grid gap-2"><Link href={localizedHref(locale, "/products")} className="group flex items-center justify-between rounded-xl bg-slate-50 px-4 py-3 text-sm font-semibold text-brand-ink transition hover:bg-brand-teal/10 hover:text-brand-blue">{zh ? "产品中心" : "Products"}<ArrowRight size={16} className="transition group-hover:translate-x-1" /></Link><Link href={localizedHref(locale, "/solutions")} className="group flex items-center justify-between rounded-xl bg-slate-50 px-4 py-3 text-sm font-semibold text-brand-ink transition hover:bg-brand-teal/10 hover:text-brand-blue">{zh ? "解决方案" : "Solutions"}<ArrowRight size={16} className="transition group-hover:translate-x-1" /></Link><Link href={localizedHref(locale, "/cases")} className="group flex items-center justify-between rounded-xl bg-slate-50 px-4 py-3 text-sm font-semibold text-brand-ink transition hover:bg-brand-teal/10 hover:text-brand-blue">{zh ? "客户案例" : "Case studies"}<ArrowRight size={16} className="transition group-hover:translate-x-1" /></Link></div></div></aside></div></div></article>; }
