import type { Metadata } from "next";
import Link from "next/link";
import { ArrowRight, Clock3, Mail, MapPin, Phone, Sparkles } from "lucide-react";
import { notFound } from "next/navigation";
import { ContactForm } from "@/components/contact/ContactForm";
import { isLocale } from "@/lib/i18n/config";
import { localizedHref } from "@/lib/i18n/routing";
import { createMetadata } from "@/lib/seo/metadata";
import { getContactPage, getGlobal } from "@/lib/strapi/queries";

export async function generateMetadata({ params }: { params: Promise<{ locale: string }> }): Promise<Metadata> {
  const { locale } = await params;
  if (!isLocale(locale)) return {};
  const [contact, global] = await Promise.all([getContactPage(locale), getGlobal(locale)]);
  return createMetadata(contact?.seo, { title: contact?.title ?? (locale === "zh" ? "联系我们" : "Contact us"), description: global?.siteDescription ?? undefined });
}

export default async function ContactPage({ params }: { params: Promise<{ locale: string }> }) {
  const { locale } = await params;
  if (!isLocale(locale)) notFound();
  const contact = await getContactPage(locale);
  if (!contact) return <section className="mx-auto max-w-3xl px-4 py-24 text-center"><h1 className="text-3xl font-semibold text-brand-ink">{locale === "zh" ? "联系我们" : "Contact us"}</h1><p className="mt-4 text-brand-muted">{locale === "zh" ? "内容暂不可用" : "Content unavailable"}</p></section>;

  const zh = locale === "zh";
  const labels = { contact: zh ? "联系我们" : "Contact us", home: zh ? "首页" : "Home", details: zh ? "联系信息" : "Contact details", address: zh ? "公司地址" : "Address", phone: zh ? "电话" : "Phone", email: "Email", hours: zh ? "办公时间" : "Office hours", response: zh ? "我们通常会在 1 个工作日内回复" : "We usually reply within one business day", map: zh ? "找到我们" : "Find us" };

  return <article className="relative overflow-hidden bg-[linear-gradient(180deg,#eef5f7_0%,#f7fafb_34rem,#f7fafb_100%)]"><div aria-hidden="true" className="pointer-events-none absolute -left-40 top-40 h-96 w-96 rounded-full bg-brand-teal/10 blur-3xl" /><div className="relative mx-auto max-w-[1720px] px-4 pb-20 pt-8 sm:px-6 sm:pt-12 lg:px-8 lg:pb-28">
    <nav aria-label={zh ? "面包屑导航" : "Breadcrumb navigation"} className="flex items-center gap-2 text-sm text-brand-muted"><Link href={localizedHref(locale, "/")} className="transition hover:text-brand-blue">{labels.home}</Link><ArrowRight size={14} className="text-brand-teal" aria-hidden="true" /><span aria-current="page" className="font-semibold text-brand-ink">{labels.contact}</span></nav>
    <header className="relative mt-10 overflow-hidden rounded-[2rem] bg-brand-ink text-white shadow-[0_24px_70px_rgba(18,50,74,0.18)]"><div aria-hidden="true" className="absolute -right-24 -top-40 h-[28rem] w-[28rem] rounded-full bg-brand-teal/25 blur-3xl" /><div aria-hidden="true" className="absolute -bottom-48 left-1/3 h-80 w-80 rounded-full bg-brand-blue/25 blur-3xl" /><div className="relative grid lg:grid-cols-[minmax(0,1.15fr)_minmax(18rem,0.85fr)]"><div className="px-6 py-12 sm:px-10 sm:py-16 lg:px-16 lg:py-20"><p className="flex items-center gap-3 text-xs font-semibold uppercase tracking-[0.24em] text-brand-teal sm:text-sm"><Sparkles size={15} aria-hidden="true" />{labels.contact}</p><h1 className="mt-5 max-w-3xl text-4xl font-semibold leading-[1.08] tracking-[-0.03em] sm:text-5xl lg:text-6xl">{contact.title ?? labels.contact}</h1>{contact.intro ? <p className="mt-6 max-w-2xl text-base leading-8 text-slate-300 sm:text-lg">{contact.intro}</p> : null}<div className="mt-9 flex flex-wrap items-center gap-x-6 gap-y-3 text-sm text-slate-300"><span className="inline-flex items-center gap-2"><span className="h-2 w-2 rounded-full bg-brand-lime" aria-hidden="true" />{labels.response}</span>{contact.phone ? <a href={`tel:${contact.phone}`} className="inline-flex items-center gap-2 font-semibold text-white transition hover:text-brand-lime">{contact.phone}<ArrowRight size={15} aria-hidden="true" /></a> : null}</div></div><div className="relative hidden min-h-[18rem] border-l border-white/10 bg-white/[0.03] p-8 lg:flex lg:flex-col lg:justify-end lg:p-12"><div className="absolute right-12 top-12 h-24 w-24 rounded-full border border-brand-teal/40" /><div className="absolute right-[4.5rem] top-[4.5rem] h-12 w-12 rounded-full border border-brand-lime/70" /><p className="relative max-w-xs text-2xl font-semibold leading-tight text-white">{zh ? "让每一次沟通，都更接近可靠的解决方案。" : "Every conversation brings us closer to a reliable solution."}</p><p className="relative mt-4 text-sm leading-6 text-slate-400">{zh ? "告诉我们您的需求，专业团队将为您提供针对性的建议。" : "Share your needs and our team will come back with focused recommendations."}</p></div></div></header>
    <div className="mt-8 grid gap-8 lg:grid-cols-[minmax(18rem,0.78fr)_minmax(0,1.22fr)] lg:gap-10"><section className="rounded-[1.75rem] border border-brand-border/80 bg-white/90 p-6 shadow-[0_18px_50px_rgba(18,50,74,0.07)] sm:p-9 lg:p-10"><div className="flex items-start justify-between gap-4 border-b border-brand-border/80 pb-6"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-brand-teal">{labels.contact}</p><h2 className="mt-2 text-2xl font-semibold text-brand-ink">{labels.details}</h2></div><span className="rounded-full bg-brand-teal/10 px-3 py-1 text-xs font-semibold text-brand-teal">{zh ? "随时联系" : "Here to help"}</span></div><dl className="mt-8 space-y-7">{contact.address ? <div className="flex gap-4"><span className="flex h-10 w-10 shrink-0 items-center justify-center rounded-xl bg-brand-teal/10 text-brand-teal"><MapPin size={19} aria-hidden="true" /></span><div className="min-w-0"><dt className="text-xs font-semibold uppercase tracking-[0.14em] text-brand-muted">{labels.address}</dt><dd className="mt-2 leading-6 text-brand-ink">{contact.address}</dd></div></div> : null}{contact.phone ? <div className="flex gap-4"><span className="flex h-10 w-10 shrink-0 items-center justify-center rounded-xl bg-brand-teal/10 text-brand-teal"><Phone size={19} aria-hidden="true" /></span><div><dt className="text-xs font-semibold uppercase tracking-[0.14em] text-brand-muted">{labels.phone}</dt><dd className="mt-2"><a href={`tel:${contact.phone}`} className="font-medium text-brand-ink transition hover:text-brand-blue">{contact.phone}</a></dd></div></div> : null}{contact.email ? <div className="flex gap-4"><span className="flex h-10 w-10 shrink-0 items-center justify-center rounded-xl bg-brand-teal/10 text-brand-teal"><Mail size={19} aria-hidden="true" /></span><div className="min-w-0"><dt className="text-xs font-semibold uppercase tracking-[0.14em] text-brand-muted">{labels.email}</dt><dd className="mt-2 break-words"><a href={`mailto:${contact.email}`} className="font-medium text-brand-ink transition hover:text-brand-blue">{contact.email}</a></dd></div></div> : null}{contact.officeHours ? <div className="flex gap-4"><span className="flex h-10 w-10 shrink-0 items-center justify-center rounded-xl bg-brand-teal/10 text-brand-teal"><Clock3 size={19} aria-hidden="true" /></span><div><dt className="text-xs font-semibold uppercase tracking-[0.14em] text-brand-muted">{labels.hours}</dt><dd className="mt-2 whitespace-pre-line leading-6 text-brand-ink">{contact.officeHours}</dd></div></div> : null}</dl></section><section className="rounded-[1.75rem] border border-brand-border/80 bg-white p-6 shadow-[0_18px_50px_rgba(18,50,74,0.09)] sm:p-9 lg:p-10"><div className="max-w-xl"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-brand-teal">{zh ? "开启合作" : "Let’s connect"}</p><h2 className="mt-2 text-2xl font-semibold tracking-tight text-brand-ink sm:text-3xl">{contact.formTitle ?? (zh ? "开始沟通" : "Start a conversation")}</h2>{contact.formIntro ? <p className="mt-3 leading-7 text-brand-muted">{contact.formIntro}</p> : null}</div><ContactForm locale={locale} /></section></div>
    {contact.mapEmbedUrl ? <section className="mt-8 overflow-hidden rounded-[1.75rem] border border-brand-border/80 bg-white shadow-[0_18px_50px_rgba(18,50,74,0.07)]"><div className="flex flex-wrap items-center justify-between gap-4 px-6 py-5 sm:px-9"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-brand-teal">{zh ? "位置" : "Location"}</p><h2 className="mt-1 text-xl font-semibold text-brand-ink">{labels.map}</h2></div><MapPin size={20} className="text-brand-teal" aria-hidden="true" /></div><iframe title={labels.map} src={contact.mapEmbedUrl} loading="lazy" referrerPolicy="no-referrer-when-downgrade" className="h-72 w-full border-0 grayscale-[0.15] sm:h-96" /></section> : null}
  </div></article>;
}
