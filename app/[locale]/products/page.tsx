import type { Metadata } from "next";
import Image from "next/image";
import Link from "next/link";
import { ArrowRight, ChevronDown, SlidersHorizontal } from "lucide-react";
import { notFound } from "next/navigation";
import { getGlobal, getProductCategories, getProducts } from "@/lib/strapi/queries";
import { isLocale } from "@/lib/i18n/config";
import { localizedHref } from "@/lib/i18n/routing";
import { resolveMediaUrl } from "@/lib/strapi/image";
import { createMetadata } from "@/lib/seo/metadata";

export async function generateMetadata({ params }: { params: Promise<{ locale: string }> }): Promise<Metadata> { const { locale } = await params; if (!isLocale(locale)) return {}; const global = await getGlobal(locale); return createMetadata(null, { title: locale === "zh" ? "产品" : "Products", description: global?.siteDescription ?? undefined }); }

export default async function ProductsPage({ params, searchParams }: { params: Promise<{ locale: string }>; searchParams: Promise<{ category?: string }> }) { const { locale } = await params; if (!isLocale(locale)) notFound(); const { category } = await searchParams; const [products, categories] = await Promise.all([getProducts(locale, category ? `filters[category][slug][$eq]=${encodeURIComponent(category)}` : ""), getProductCategories(locale)]); return <section className="mx-auto max-w-7xl px-4 py-14 sm:px-6 lg:px-8"><header className="relative overflow-hidden rounded-3xl bg-brand-ink px-6 py-12 text-white sm:px-10"><div className="absolute -right-20 -top-24 h-64 w-64 rounded-full bg-brand-teal/30 blur-3xl" /><div className="relative max-w-2xl"><p className="text-sm font-bold uppercase tracking-[0.2em] text-brand-lime">{locale === "zh" ? "产品目录" : "Product catalogue"}</p><h1 className="mt-4 text-4xl font-semibold tracking-tight sm:text-5xl">{locale === "zh" ? "为真实业务打造的产品" : "Products built for the real world"}</h1><p className="mt-5 text-lg leading-8 text-slate-200">{locale === "zh" ? "从缓冲包装到自动化设备，为每一次运输提供可靠保护。" : "From protective packaging to automation equipment, reliable solutions for every shipment."}</p></div></header><div className="brand-panel mt-8 p-4 sm:p-5"><div className="flex items-center gap-2 text-sm font-semibold text-brand-ink"><SlidersHorizontal size={18} className="text-brand-teal" />{locale === "zh" ? "按产品类别浏览" : "Browse by product category"}</div><div className="mt-4 grid grid-cols-1 gap-3 sm:grid-cols-2 lg:grid-cols-3">{<Link href={localizedHref(locale, "/products")} className={`flex items-center justify-between rounded-xl px-4 py-3 text-sm font-semibold transition ${!category ? "bg-brand-blue text-white" : "bg-slate-50 text-brand-ink hover:bg-brand-teal/10"}`}>{locale === "zh" ? "全部产品" : "All products"}<ArrowRight size={16} /></Link>}{categories.map((item) => <div key={item.id} className="group relative"><Link href={localizedHref(locale, `/products?category=${item.slug}`)} className={`flex items-center justify-between rounded-xl px-4 py-3 text-sm font-semibold transition ${category === item.slug ? "bg-brand-blue text-white" : "bg-slate-50 text-brand-ink hover:bg-brand-teal/10"}`}>{item.name}<ChevronDown size={16} className="text-brand-teal" /></Link><div className="pointer-events-none absolute left-0 right-0 top-full z-10 mt-1 rounded-xl border border-brand-border bg-white p-3 opacity-0 shadow-lg transition group-hover:pointer-events-auto group-hover:opacity-100"><p className="text-xs text-brand-muted">{item.description ?? (locale === "zh" ? "浏览该分类下的产品" : "Browse products in this category")}</p><Link href={localizedHref(locale, `/products?category=${item.slug}`)} className="brand-link mt-2 inline-block text-xs">{locale === "zh" ? "查看分类" : "View category"}</Link></div></div>)}</div></div>{products.length ? <div className="mt-10 grid gap-7 sm:grid-cols-2 lg:grid-cols-3">{products.map((product) => { const image = resolveMediaUrl(product.cover); return <article key={product.id} className="group brand-card overflow-hidden"><Link href={localizedHref(locale, `/products/${product.slug}`)} className="block focus-visible:outline-2 focus-visible:outline-brand-teal">{image ? <div className="relative aspect-[4/3] overflow-hidden bg-slate-100"><Image src={image} alt={product.cover?.alternativeText ?? product.name} fill sizes="(min-width: 1024px) 33vw, (min-width: 640px) 50vw, 100vw" className="object-cover transition duration-500 group-hover:scale-105" /></div> : <div className="aspect-[4/3] bg-brand-teal/10" />}<div className="flex min-h-40 flex-col p-6"><h2 className="text-xl font-semibold text-brand-ink">{product.name}</h2>{product.summary ? <p className="mt-3 line-clamp-3 text-sm leading-6 text-brand-muted">{product.summary}</p> : null}<span className="mt-auto flex items-center gap-2 pt-5 text-sm font-semibold text-brand-blue">{locale === "zh" ? "查看详情" : "View details"}<ArrowRight size={16} /></span></div></Link></article>; })}</div> : <div className="brand-panel mt-10 p-12 text-center"><p className="text-brand-muted">{locale === "zh" ? "该分类暂无产品" : "No products in this category yet"}</p><Link href={localizedHref(locale, "/products")} className="brand-button-primary mt-5">{locale === "zh" ? "查看全部产品" : "View all products"}</Link></div>}</section>; }
