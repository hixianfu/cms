"use client";
import { useState } from "react";
import { RichTextRenderer } from "@/components/content/RichTextRenderer";
import type { Product } from "@/types/content";
export function ProductTabs({ product, locale, blogs }: { product: Product; locale: string; blogs: { title: string; slug: string }[] }) { const [tab,setTab]=useState("details"); const zh=locale==="zh"; return <section className="mt-16"><div className="flex gap-2 border-b border-slate-200">{[["details",zh?"产品详情":"Product details"],["video",zh?"产品视频":"Product videos"],["blog",zh?"相关博客":"Related blog"]].map(([k,l])=><button key={k} onClick={()=>setTab(k)} className={`border-b-2 px-5 py-3 text-sm font-semibold ${tab===k?"border-brand-teal text-brand-blue":"border-transparent text-slate-500"}`}>{l}</button>)}</div><div className="mt-8">{tab==="details"?<RichTextRenderer blocks={product.blocks}/>:tab==="video"?<p className="rounded-xl bg-slate-50 p-8 text-center text-slate-500">{zh?"暂无产品视频":"No product videos yet"}</p>:blogs.length?<div className="grid gap-4 md:grid-cols-2">{blogs.map(b=><a key={b.slug} href={`/${locale}/blog/${b.slug}`} className="rounded-xl border p-5 font-semibold hover:border-brand-teal">{b.title}</a>)}</div>:<p className="rounded-xl bg-slate-50 p-8 text-center text-slate-500">{zh?"暂无相关文章":"No related articles yet"}</p>}</div></section>; }
