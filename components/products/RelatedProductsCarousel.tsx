"use client";
import Image from "next/image"; import Link from "next/link"; import { useState } from "react"; import type { Product } from "@/types/content"; import { resolveMediaUrl } from "@/lib/strapi/image";
export function RelatedProductsCarousel({products,locale}:{products:Product[];locale:string}){const [i,setI]=useState(0);if(!products.length)return null;const p=products[i],img=resolveMediaUrl(p.cover);return <section className="mt-20"><h2 className="text-2xl font-semibold">{locale==="zh"?"相关产品":"Related products"}</h2><div className="relative mt-6 overflow-hidden rounded-2xl border"><Link href={`/${locale}/products/${p.slug}`} className="grid md:grid-cols-2">{img?<div className="relative aspect-[4/3]"><Image src={img} alt={p.name} fill className="object-cover"/></div>:<div className="aspect-[4/3] bg-brand-teal/10"/>}<div className="p-8"><h3 className="text-2xl font-semibold">{p.name}</h3><p className="mt-3 text-slate-600">{p.summary}</p></div></Link><div className="absolute bottom-4 right-4 flex gap-2"><button onClick={e=>{e.preventDefault();setI((i-1+products.length)%products.length)}} className="rounded-full bg-white px-3 py-2 shadow">‹</button><button onClick={e=>{e.preventDefault();setI((i+1)%products.length)}} className="rounded-full bg-white px-3 py-2 shadow">›</button></div></div></section>}
