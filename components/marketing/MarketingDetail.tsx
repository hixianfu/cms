import Image from "next/image";
import Link from "next/link";
import { ArrowLeft, ArrowUpRight } from "lucide-react";
import ReactMarkdown from "react-markdown";
import remarkGfm from "remark-gfm";
import { localizedHref } from "@/lib/i18n/routing";
import { resolveMediaUrl } from "@/lib/strapi/image";
import type { Locale, Scenario, Solution } from "@/types/content";

type Ref = { slug: string; name?: string; title?: string };
function RelatedLinks({ items, locale, path }: { items?: Ref[]; locale: Locale; path: string }) { return items?.map((item) => <Link key={item.slug} href={localizedHref(locale, `/${path}/${item.slug}`)} className="group flex items-center justify-between border-b border-brand-border py-3 text-sm font-semibold text-brand-ink last:border-0"><span>{item.name ?? item.title ?? item.slug}</span><ArrowUpRight size={15} className="text-brand-teal transition group-hover:translate-x-1 group-hover:-translate-y-1" /></Link>); }
function TextSection({ title, body }: { title: string; body?: string | null }) { return body ? <section><h2 className="text-2xl font-semibold text-brand-ink">{title}</h2><p className="mt-4 whitespace-pre-line text-base leading-8 text-brand-muted">{body}</p></section> : null; }

export function MarketingDetail({ item, locale, kind }: { item: Solution | Scenario; locale: Locale; kind: "solution" | "scenario" }) {
  const solution = kind === "solution";
  const solutionItem = solution ? item as Solution : null;
  const scenarioItem = !solution ? item as Scenario : null;
  const path = solution ? "solutions" : "scenarios";
  const cover = resolveMediaUrl(item.cover);
  return <article className="mx-auto max-w-6xl px-4 py-14 sm:px-6 lg:px-8"><Link href={localizedHref(locale, `/${path}`)} className="brand-link inline-flex items-center gap-2 text-sm"><ArrowLeft size={16} />{locale === "zh" ? (solution ? "返回解决方案" : "返回应用场景") : (solution ? "Back to solutions" : "Back to scenarios")}</Link><header className="mx-auto mt-10 max-w-4xl"><p className="text-sm font-bold uppercase tracking-[0.2em] text-brand-teal">{item.industry ?? (locale === "zh" ? (solution ? "解决方案" : "应用场景") : (solution ? "Solution" : "Application scenario"))}</p><h1 className="mt-4 text-4xl font-semibold tracking-tight text-brand-ink sm:text-6xl">{item.title}</h1><p className="mt-6 max-w-3xl text-lg leading-8 text-brand-muted">{item.summary}</p></header>{cover ? <div className="relative mt-12 aspect-[16/8] overflow-hidden rounded-3xl shadow-lg"><Image src={cover} alt={item.cover?.alternativeText ?? item.title} fill priority sizes="(min-width: 1024px) 1152px, 100vw" className="object-cover" /></div> : null}<div className="mt-14 grid gap-12 lg:grid-cols-[minmax(0,1fr)_20rem]"><div className="space-y-10">{solutionItem ? <><TextSection title={locale === "zh" ? "客户痛点" : "Customer pain points"} body={solutionItem.customerPain} /><TextSection title={locale === "zh" ? "包装需求" : "Packaging needs"} body={solutionItem.packagingNeeds} /></> : null}{item.content ? <section><h2 className="text-2xl font-semibold text-brand-ink">{locale === "zh" ? (solution ? "方案详情" : "场景详情") : (solution ? "Solution details" : "Scenario details")}</h2><div className="prose prose-slate mt-4 max-w-none"><ReactMarkdown remarkPlugins={[remarkGfm]}>{item.content}</ReactMarkdown></div></section> : null}</div><aside className="space-y-6"><div className="brand-panel p-5"><h2 className="text-sm font-bold uppercase tracking-[0.16em] text-brand-teal">{locale === "zh" ? "相关内容" : "Related content"}</h2>{item.products?.length ? <div className="mt-4"><p className="text-xs font-semibold text-brand-muted">{locale === "zh" ? "相关产品" : "Products"}</p><RelatedLinks items={item.products} locale={locale} path="products" /></div> : null}{item.cases?.length ? <div className="mt-4"><p className="text-xs font-semibold text-brand-muted">{locale === "zh" ? "客户案例" : "Cases"}</p><RelatedLinks items={item.cases} locale={locale} path="cases" /></div> : null}{item.videos?.length ? <div className="mt-4"><p className="text-xs font-semibold text-brand-muted">{locale === "zh" ? "相关视频" : "Videos"}</p><RelatedLinks items={item.videos} locale={locale} path="videos" /></div> : null}{item.articles?.length ? <div className="mt-4"><p className="text-xs font-semibold text-brand-muted">{locale === "zh" ? "相关文章" : "Articles"}</p><RelatedLinks items={item.articles} locale={locale} path="blog" /></div> : null}{solutionItem?.scenarios?.length ? <div className="mt-4"><p className="text-xs font-semibold text-brand-muted">{locale === "zh" ? "应用场景" : "Scenarios"}</p><RelatedLinks items={solutionItem.scenarios} locale={locale} path="scenarios" /></div> : null}{scenarioItem?.solutions?.length ? <div className="mt-4"><p className="text-xs font-semibold text-brand-muted">{locale === "zh" ? "相关方案" : "Solutions"}</p><RelatedLinks items={scenarioItem.solutions} locale={locale} path="solutions" /></div> : null}</div><div className="rounded-2xl bg-brand-blue p-6 text-white"><h2 className="text-xl font-semibold">{locale === "zh" ? "需要专业建议？" : "Need expert advice?"}</h2><p className="mt-3 text-sm leading-6 text-blue-100">{locale === "zh" ? "联系我们，获取适合你业务的包装建议。" : "Talk with our team about the right packaging approach for your operation."}</p><Link href={localizedHref(locale, "/contact")} className="brand-button-accent mt-5">{locale === "zh" ? "立即询盘" : "Start an inquiry"}<ArrowUpRight size={16} /></Link></div></aside></div></article>;
}
